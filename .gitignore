# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (Vercel writes the OIDC token here via `vercel env pull`)
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
.env*

# ...but the example file ships with the template (no secrets in it)
!.env.local.example
